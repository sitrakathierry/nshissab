 {% extends 'menu/index.html.twig' %}

{% block title %}{{titlePage}}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{asset('tools/personnal/systeme/' ~ filename ~ '/css/' ~ filename ~ '.css')}}">
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{asset('tools/personnal/systeme/' ~ filename ~ '/js/' ~ filename ~ '.js')}}" ></script>
{% endblock %}
{% block body %}
<div class="barre_top rounded shadow mb-5">
    <div class="w-100 border-bottom pt-2 px-3">
        <h4 class="text-uppercase entete_page text-black">{{titlePage}}</h4>
    </div>
    <div class="container-fluid px-3 pt-3">
        <div class="row">
            <div class="col-3">
                <label for="coiff_search_employee" class="font-weight-bold">Nom</label>
                <select name="coiff_search_employee" class="custom-select chosen_select custom-select-sm" id="coiff_search_employee">
                    <option value="">-</option>
                    {# {% for categorie in categories %}
                        <option value="{{categorie.id}}" >{{categorie.nom|upper}}</option>
                    {% endfor %}  #}
                </select>
            </div>
        </div>
        <div class="table-responsive mt-4 limit_tbody">
            <table class="table table-sm table-bordered">
                <thead class="thead-dark">
                    <tr>
                        <th>Coiffeur</th>
                        <th>Montant</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    {% for dataSuivi in dataSuivis %}
                        <tr>
                            <td>{{dataSuivi.nom|upper}}</td>
                            <td>{{dataSuivi.montant}}</td>
                            <td class="text-center align-middle">
                                <button type="button" class="btn btn-sm mx-1 font-smaller btn-outline-warning"><i class="fa fa-edit"></i></button>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
        {% if with_foot %}
            <div class="foot_action p-3 mt-4 text-right w-100 barre_dashed">
                <button type="submit" class="btn btn-sm ml-3 btn-perso-one"><i class="fa fa-save"></i>&nbsp;Enregistrer</button>
            </div>
        {% endif %} 
    </div>
</div>
{% endblock %}


